using AutoMapper;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using System.Collections.Generic;
using WeatherMonitor.Web.Contracts;
using WeatherMonitor.Web.Controllers;
using WeatherMonitor.Web.Data;
using WeatherMonitor.Web.Models;

namespace WeatherMonitor.UnitTests
{
    [TestClass]
    public class WeatherMonitorUnitTests
    {
        [TestMethod]
        public void GetWeatherEntries()
        {
            // Arrange
            //var controller = new WeatherEntriesController(repository);
            //controller.Request = new HttpRequestMessage();
            //controller.Configuration = new HttpConfiguration();

            // Act
            //var response = controller.Get(10);

            // Assert
            //List<WeatherEntry> weatherEntries;
            //Assert.IsTrue(response.TryGetContentValue<Product>(out product));
            //Assert.AreEqual(10, product.Id);
        }
    }
}
